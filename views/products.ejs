<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hello World Products</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <%- include('./partials/nav'); %>
    <%- include('./partials/reviewModal'); %>
    <div class="container">
        <div class="row">
            <% mobiles.forEach(mobile => { %>
                <div class="col-md-4 mb-3">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title"> Brand: <%= mobile.Brand %></h5>
                            <p class="card-text"> Model: <%= mobile.ModelName %></p>
                            <p class="card-text"> Os: <%= mobile.ModelName %></p>
                            <p class="card-text"> Release: <%= mobile.Release %></p>
                        </div>
                    </div>
                </div>
            <% }); %>
        </div>
    </div>
    <footer>
        <%- include('./partials/footer'); %>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
